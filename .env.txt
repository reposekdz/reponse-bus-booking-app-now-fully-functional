# GoBus Environment Variables Configuration
# Copy the relevant sections into the correct .env files in your project.

# ===============================================
# == BACKEND CONFIGURATION (backend/.env file) ==
# ===============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL for CORS and Socket.IO
FRONTEND_URL=http://localhost:3000

# Database Configuration
# Make sure your MySQL database is running and you have created a 'gobus' database.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=gobus

# JWT Authentication
# IMPORTANT: Use a long, random, and secret string for production
JWT_SECRET=replace_with_a_long_random_secret_string
JWT_EXPIRES_IN=30d

# Web Push Notifications (VAPID Keys)
# Generate these using the command: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Mock API Keys for external services (e.g., MTN Mobile Money Sandbox)
MTN_API_KEY=mock-mtn-api-key
MTN_USER_ID=mock-mtn-user-id


# =======================================================
# == FRONTEND CONFIGURATION (root .env.local file) ==
# =======================================================

# Public key for VAPID web push notifications.
# This key is safe to be exposed to the frontend.
# It should match the VAPID_PUBLIC_KEY in the backend .env file.
VITE_VAPID_PUBLIC_KEY=YOUR_VAPID_PUBLIC_KEY_HERE
