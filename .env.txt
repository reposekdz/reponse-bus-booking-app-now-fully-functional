
# GoBus Environment Variables Configuration
# Copy the relevant sections into the correct .env files in your project.

# ===============================================
# == BACKEND CONFIGURATION (backend/.env file) ==
# ===============================================
# Create this file in the 'backend' directory for both development and production.

# --- Server Configuration ---
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:3000

# --- Database Configuration ---
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=gobus

# --- JWT Authentication ---
JWT_SECRET=your_super_secret_jwt_key_here_that_is_very_long

# --- Web Push Notifications (VAPID Keys) ---
VAPID_PUBLIC_KEY=YOUR_GENERATED_VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY=YOUR_GENERATED_VAPID_PRIVATE_KEY

# --- MTN MoMo API Keys (Production & Sandbox) ---

# Collection API (Receiving Payments)
MTN_COLLECTION_USER_ID=your_collection_user_id
MTN_COLLECTION_API_KEY=your_collection_api_key
MTN_COLLECTIONS_PRIMARY_KEY=8c1c562bfbe241458e3b0bdc4d05d40e
MTN_COLLECTIONS_SECONDARY_KEY=13b6baa354c044f9a0159b74642b7791

# Disbursement API (Sending Payouts/Refunds)
MTN_DISBURSEMENT_USER_ID=your_disbursement_user_id
MTN_DISBURSEMENT_API_KEY=your_disbursement_api_key
MTN_DISBURSEMENTS_PRIMARY_KEY=a34dd9b3f5b74e729fddf3cf47941795
MTN_DISBURSEMENTS_SECONDARY_KEY=6ff111de84b5464492ce970caf16fa30

# Remittance API (Transfers)
MTN_REMITTANCE_USER_ID=your_remittance_user_id
MTN_REMITTANCE_API_KEY=your_remittance_api_key
MTN_REMITTANCE_PRIMARY_KEY=your_remittance_primary_key


# =======================================================
# == FRONTEND CONFIGURATION (root .env.local file) =====
# =======================================================
# Create this file in the project's root directory.

# --- Public key for VAPID web push notifications ---
VITE_VAPID_PUBLIC_KEY=YOUR_GENERATED_VAPID_PUBLIC_KEY

# --- Google Gemini & Maps Grounding ---
VITE_GOOGLE_API_KEY=YOUR_GOOGLE_AI_API_KEY
